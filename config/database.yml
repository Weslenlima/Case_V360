# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  port: 5432
  username: postgres
  password: Selamento17*
  pool: <%= ENV.fetch("RAILS_MAX_THREADS"){5} %>


development:
  <<: *default
  database: v360_case_development


test:
  <<: *default
  database: v360_case_test


production:
 
 <<: *default
 
 database: <%= ENV.fetch("V360_CASE_DATABASE_NAME") %> 
 host: <%= ENV.fetch("V360_CASE_DATABASE_HOST") %> 
 username: <%= ENV.fetch("V360_CASE_DATABASE_USER") %> 
 password: <%= ENV.fetch("V360_CASE_DATABASE_PASSWORD") %> 
 port: <%= ENV.fetch("V360_CASE_DATABASE_PORT") %>

